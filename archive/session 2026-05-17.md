# New Session Initialization

Copy and paste this exactly to start a new session:

---

I'm working on a Chrome extension called "Wordle Buddy" located at `/Users/corey/Documents/Categories/Projects/extensions/wordle-buddy/`.

The extension is **fully functional and deployed**. It generates permutations for Wordle puzzles of any length (4-7 letters) using a layered Green/Yellow/Gray constraint approach. The core algorithm correctly handles repeated letters using a "maxKnown" pattern where we track the maximum known occurrences across all guesses, then subtract greens to get yellows.

Please read the following files for context:
1. `/Users/corey/Documents/Categories/Projects/extensions/wordle-buddy/QUICK_REFERENCE.md` - Quick overview
2. `/Users/corey/Documents/Categories/Projects/extensions/wordle-buddy/SESSION_HANDOFF.md` - Complete technical details

The extension is working perfectly. All core features are tested and functional:
- Variable word length detection (4-7 letters)
- Accurate permutation generation
- Correct repeated letter handling (e.g., THEME/EERIE/SCENE test case)
- User notes persistence
- Excluded letters display
- Clean, minimal UI

Key technical context:
- User is Hugh (prefers iterative development, values accuracy over speed)
- Core algorithm is in `permutationEngine.js` (especially the `processGuess()` method)
- The maxKnown pattern is critical: `yellows[letter].count = maxKnown - greensCount`
- Code is well-documented with inline comments

**My request**:
1. Fix bug in permutation generation where a permutation includes a letter in an invalid location. (To reproduce: with answer NEWLY, guess ADIEU, STORY, ELEGY; in ELEGY, the E in the third position is Gray, indicating that position cannot contain that letter; but permutations incorrectly include L_E_Y and __ELY.)
2. When only one possible permutation remains, display a message (e.g., "You know all you need to know!") instead of the completed word. The users want that victorious feeling of putting the last pieces together.
3. Make the panel movable and resizable (preferably by dragging). (If not possible or reliable, discuss options before starting changes.)
4. Vertically compress the list of permutations. Make the boxes around the individual words smaller, and maybe the padding between them, or remove the boxes entirely. Ideally, all permutations would be visible at once, with the text area filling the remaining space.
5. Fit the panel more snugly into the side of the window, where it is less likely to block the puzzle.
  1. Make the default width of the panel about half the current width.
  2. Make the default height of the panel close to the full height of the window, leaving room for the menu bar at the top.
  3. Bind the text area height and width (with padding) to the panel frame. As mentioned above, the intent is to have a dynamic list of permutations on top, and a text area that expands to fill the remaining space on bottom.
6. Implement customizable settings. Allow users to specify various display traits.
  1. Zoom (relative to window zoom)
  2. Force color scheme: allow user to force panel into Light mode, Dark mode, or default/mimicking the game page.
  3. Toggle a Details section, showing the information currently in "Constraint Details"
  4. Toggle section headers (remove "Possible Permutations" and "Your Notes" and "Details" titles).
  5. Allow selecting different font & font qualities for permutations
  6. Allow selecting different font & font qualities for text area
7. Use semver-style versioning; set this version to 0.2

---

**Important**: This is a handoff to continue working on an existing, functional project. Do not suggest rebuilding from scratch or changing the core algorithm unless there's a confirmed bug.
